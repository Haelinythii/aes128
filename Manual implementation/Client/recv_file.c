#include<stdio.h>
#include<stdlib.h>
#include<string.h>
#include<sys/socket.h>
#include<arpa/inet.h>
#include<inttypes.h>
#include<unistd.h>
#include<time.h>

#define PORT 8080
#define BUFFER_SIZE 1024
#define BLOCK_SIZE 16
#define EXPANDED_KEY_SIZE 44
#define KEY_PAIR_SIZE 2

uint8_t sBox[] = {
    
};

uint8_t iBox[256] = {
    
};

uint8_t rCon[10] = {
    0x01, 0x02, 0x04, 0x08, 0x10,
    0x20, 0x40, 0x80, 0x1B, 0x36
};

uint8_t columnTransformation[] = {
    0x0E, 0x0B, 0x0D, 0x09,
    0x09, 0x0E, 0x0B, 0x0D,
    0x0D, 0x09, 0x0E, 0x0B,
    0x0B, 0x0D, 0x09, 0x0E
};

void transposeMatrix(uint8_t* matrix){
    int i, j;
    uint8_t temp;
    for(i=0; i<4; i++){
        for(j=i+1; j<4; j++){
            temp = *(matrix + i * 4 + j);
            *(matrix + i * 4 + j) = *(matrix + j * 4 + i);
            *(matrix + j * 4 + i) = temp;
        }
    }
}

void addRoundKey(uint8_t* blockState, uint32_t* roundKey){
    int i, j;
    for(i=0; i<4; i++){
        uint8_t
            wi0 =  *(roundKey + i) >> 24,
            wi1 = (*(roundKey + i) >> 16) & 0xFF,
            wi2 = (*(roundKey + i) >> 8)  & 0xFF,
            wi3 =  *(roundKey + i)        & 0xFF;
        
        *(blockState + i) = *(blockState + i) ^ wi0;
        *(blockState + 4 + i) = *(blockState + 4 + i) ^ wi1;
        *(blockState + 8 + i) = *(blockState + 8 + i) ^ wi2;
        *(blockState + 12 + i) = *(blockState + 12 + i) ^ wi3;
    }
}

uint8_t gf8Multiply(uint8_t a, uint8_t b){
    uint8_t res = 0;
    while(b > 0x00){
        if(b & 1){
            res = res ^ a;
        }
        uint8_t msb = a & 0x80;
        a = a << 1;
        if(msb)
            a = a ^ 0x1B;

        b = b >> 1;
    }
    return res;
}

void mixColumn(uint8_t* blockState){
    int i, j;
    for(i=0; i<4; i++){
        uint8_t
            s0 = *(blockState + i),
            s1 = *(blockState + i + 4),
            s2 = *(blockState + i + 8),
            s3 = *(blockState + i + 12);
        for(j=0; j<4; j++){
            uint8_t
                a = gf8Multiply(s0, columnTransformation[j*4 + 0]),
                b = gf8Multiply(s1, columnTransformation[j*4 + 1]),
                c = gf8Multiply(s2, columnTransformation[j*4 + 2]),
                d = gf8Multiply(s3, columnTransformation[j*4 + 3]);
            *(blockState + j * 4 + i) = a ^ b ^ c ^ d;
        }
    }
}

void invShiftRow(uint8_t* blockState){
    uint8_t temp = *(blockState + 4 + 3);
    *(blockState + 4 + 3) = *(blockState + 4 + 2);
    *(blockState + 4 + 2) = *(blockState + 4 + 1);
    *(blockState + 4 + 1) = *(blockState + 4 + 0);
    *(blockState + 4 + 0) = temp;

    temp = *(blockState + 8 + 0);
    *(blockState + 8 + 0) = *(blockState + 8 + 2);
    *(blockState + 8 + 2) = temp;

    temp = *(blockState + 8 + 1);
    *(blockState + 8 + 1) = *(blockState + 8 + 3);
    *(blockState + 8 + 3) = temp;
    
    temp = *(blockState + 12 + 0);
    *(blockState + 12 + 0) = *(blockState + 12 + 1);
    *(blockState + 12 + 1) = *(blockState + 12 + 2);
    *(blockState + 12 + 2) = *(blockState + 12 + 3);
    *(blockState + 12 + 3) = temp;
}

void invSubByte(uint8_t* blockState){
    int i;
    for(i=0; i<BLOCK_SIZE; i++)
        *(blockState + i) = iBox[*(blockState + i)];
}

uint8_t* getDecryptedFileContent(uint8_t* fileContent, uint32_t* expandedKey){
    int i;
    addRoundKey(fileContent, expandedKey + 40);

    for(i=0; i<10; i++){
        invShiftRow(fileContent);
        invSubByte(fileContent);
        addRoundKey(fileContent, expandedKey + 36 - 4 * i);
        if(i < 9)
            mixColumn(fileContent);
    }
    transposeMatrix(fileContent);
    return fileContent;
}

uint32_t* getExpandedKey(uint8_t* uniqueKey){
    uint32_t* w = (uint32_t*)malloc(EXPANDED_KEY_SIZE * sizeof(uint32_t));
    int i;
    for(i=0; i<4; i++)
        *(w + i) =
            (*(uniqueKey + i * 4 + 0) << 24) + (*(uniqueKey + i * 4 + 1) << 16) + (*(uniqueKey + i * 4 + 2) << 8) + (*(uniqueKey + i * 4 + 3));

    for(i=4; i<44; i++){
        uint32_t temp = w[i-1];
        if((i%4) == 0){
            uint8_t
                b0 =  temp >> 24,
                b1 = (temp >> 16) & 0xFF,
                b2 = (temp >> 8)  & 0xFF,
                b3 =  temp        & 0xFF;
            uint8_t
                s0 = sBox[b1],
                s1 = sBox[b2],
                s2 = sBox[b3],
                s3 = sBox[b0];
            s0 = s0 ^ rCon[(i/4)-1];
            temp = (s0 << 24) + (s1 << 16) + (s2 << 8) + s3;
        }
        *(w + i) = *(w + i - 4) ^ temp;
    }
    return w;
}

int modExp(int a, int pow, int mod)
{
	int res = 1;

	a = a % mod;

	if (a == 0)
        return 0;

	while (pow > 0)
	{
		if (pow & 1)
			res = (res * a) % mod;

		pow = pow>>1;
		a = (a*a) % mod;
	}
	return res;
}

uint8_t* recvUniqueKey(int socket){
    int* keyPair = (int*)malloc(KEY_PAIR_SIZE * sizeof(int));
    int* encryptedUniqueKey = (int*)malloc(BLOCK_SIZE * sizeof(int));

    recv(socket, keyPair, KEY_PAIR_SIZE * sizeof(int), 0);
    recv(socket, encryptedUniqueKey, BLOCK_SIZE * sizeof(int), 0);

    uint8_t* uniqueKey = (uint8_t*)malloc(BLOCK_SIZE * sizeof(uint8_t));
    int i;
    for(i=0; i<BLOCK_SIZE; i++)
        *(uniqueKey + i) = modExp(*(encryptedUniqueKey + i), *(keyPair), *(keyPair + 1));
    return uniqueKey;
}

void recvFile(int socket, char* filename, uint8_t* uniqueKey){
    recv(socket, filename, BUFFER_SIZE, 0);
    FILE * fp = fopen(filename, "wb");
    
    uint8_t* fileContent;
    fileContent = (uint8_t*)malloc(BUFFER_SIZE * sizeof(uint8_t));
    memset(fileContent, 0, BUFFER_SIZE);

    uint32_t* expandedKey = getExpandedKey(uniqueKey);

    int* len = (int*)malloc(sizeof(int));

    while(recv(socket, fileContent, BLOCK_SIZE, 0) > 0){
        recv(socket, len, sizeof(int), 0);
        uint8_t* decryptedFileContent = getDecryptedFileContent(fileContent, expandedKey);
        
        fwrite(decryptedFileContent, sizeof(uint8_t), *(len), fp);
        
        memset(fileContent, 0, BLOCK_SIZE);
    }
    fclose(fp);
    printf("Received file: %s\n", filename);
}

int main(int argc, char const *argv[])
{
    int sock = 0, valread;
    struct sockaddr_in serv_addr;
    char *hello = "File sent successfully.";
    char *reply = "Content received.";
    char filename[BUFFER_SIZE];
    memset(filename, 0, BUFFER_SIZE);
    
    if ((sock = socket(AF_INET, SOCK_STREAM, 0)) < 0)
    {
        printf("\n Socket creation error \n");
        return -1;
    }
   
    serv_addr.sin_family = AF_INET;
    serv_addr.sin_port = htons(PORT);
       
    // Convert IPv4 and IPv6 addresses from text to binary form
    if(inet_pton(AF_INET, "127.0.0.1", &serv_addr.sin_addr)<=0) 
    {
        printf("\nInvalid address/ Address not supported \n");
        return -1;
    }
   
    if (connect(sock, (struct sockaddr *)&serv_addr, sizeof(serv_addr)) < 0)
    {
        printf("\nConnection Failed \n");
        return -1;
    }

    clock_t begin, end;
    begin = clock();

    uint8_t* uniqueKey = recvUniqueKey(sock);
    recvFile(sock, filename, uniqueKey);

    end = clock();

    double deltaTime = ((double)(end-begin)) / CLOCKS_PER_SEC;
    printf("Running time: %lfs...\n", deltaTime);

    close(sock);
    return 0;
}